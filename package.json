{
  "name": "painted-firmament",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive constellation mythology explorer: a live star map with scientific and mythic views.",
  "scripts": {
    "build": "node build.js",
    "frames": "cd tools && node frames.js",
    "start": "node build.js && npx serve dist"
  },
  "engines": { "node": ">=18" }
}
